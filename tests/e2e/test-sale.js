const fetch = require('node-fetch');

async function test() {
  console.log("Starting test...");
  
  // Create user directly through DB so we don't have to fiddle with OTP and Redis
  // Wait, let's just use the api URL to sign up. Wait, OTP is hard to read from node.
}
test();
